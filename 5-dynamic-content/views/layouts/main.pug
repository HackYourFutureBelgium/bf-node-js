doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="./css/style.css")
        title #{title}
    body 
        header 
            nav 
                ul 
                    li 
                        a(href="/", class=(path === '/' ? 'active' : '')) Shop
                        a(href="/products", class=(path === '/addProduct' ? 'active' : '')) Add Product 
        block content 